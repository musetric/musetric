export type SpectrogramColors = {
  foreground: string;
  background: string;
  primary: string;
  recordingForeground: string;
  recordingMatch: string;
  recordingClose: string;
  recordingMiss: string;
  recordingTimingMiss: string;
};

diff --git a/dist/ConfigurationChief.js b/dist/ConfigurationChief.js
index 760084b03ec799ea2c91ea53e2ca5c368344516f..5f385574c41b88decb7347e8b5f04cb217b43159 100644
--- a/dist/ConfigurationChief.js
+++ b/dist/ConfigurationChief.js
@@ -97,6 +97,7 @@ export class ConfigurationChief {
         return hints;
     }
     normalize(rawConfig) {
+        const conditions = rawConfig.conditions;
         const ignore = arrayify(rawConfig.ignore ?? defaultConfig.ignore);
         const ignoreFiles = arrayify(rawConfig.ignoreFiles ?? defaultConfig.ignoreFiles);
         const ignoreBinaries = rawConfig.ignoreBinaries ?? [];
@@ -116,6 +117,7 @@ export class ConfigurationChief {
             }
         }
         return {
+            ...(conditions && { conditions }),
             ignore,
             ignoreFiles,
             cycles,
diff --git a/dist/run.js b/dist/run.js
index 0d329c483725e66dadf28725184941de69ef675e..15bda80496e5df94bb3bf3268307b0e1b59779b2 100644
--- a/dist/run.js
+++ b/dist/run.js
@@ -12,6 +12,7 @@ import { debugLogObject } from './util/debug.js';
 import { flushGitignoreCache, initGitignoreCache } from './util/gitignore-cache.js';
 import { flushGlobCache, initGlobCache } from './util/glob-cache.js';
 import { getGitIgnoredHandler } from './util/glob-core.js';
+import { setResolverConditions } from './util/resolve.js';
 import { getModuleSourcePathHandler, getWorkspaceManifestHandler } from './util/to-source-path.js';
 import { getSessionHandler } from './util/watch.js';
 export const run = async (options) => {
@@ -22,6 +23,7 @@ export const run = async (options) => {
         initGitignoreCache(options.cacheLocation);
     }
     const chief = new ConfigurationChief(options);
+    setResolverConditions(chief.config.conditions);
     const deputy = new DependencyDeputy(options);
     const streamer = new ConsoleStreamer(options);
     const collector = new IssueCollector(options);
diff --git a/dist/schema/configuration.d.ts b/dist/schema/configuration.d.ts
index 72d14854d22461cfa5502aabac73bc3bd12aae9f..b2bb382227711e0fad8d69e23da2f85151799302 100644
--- a/dist/schema/configuration.d.ts
+++ b/dist/schema/configuration.d.ts
@@ -1778,6 +1778,7 @@ export declare const knipConfigurationSchema: z.ZodMiniObject<{
         project: z.ZodMiniOptional<z.ZodMiniUnion<readonly [z.ZodMiniString<string>, z.ZodMiniArray<z.ZodMiniString<string>>]>>;
     }, z.core.$strip>]>>;
     $schema: z.ZodMiniOptional<z.ZodMiniString<string>>;
+    conditions: z.ZodMiniOptional<z.ZodMiniArray<z.ZodMiniString<string>>>;
     rules: z.ZodMiniOptional<z.ZodMiniRecord<z.ZodMiniUnion<readonly [z.ZodMiniLiteral<"files">, z.ZodMiniLiteral<"dependencies">, z.ZodMiniLiteral<"devDependencies">, z.ZodMiniLiteral<"optionalPeerDependencies">, z.ZodMiniLiteral<"unlisted">, z.ZodMiniLiteral<"binaries">, z.ZodMiniLiteral<"unresolved">, z.ZodMiniLiteral<"exports">, z.ZodMiniLiteral<"types">, z.ZodMiniLiteral<"nsExports">, z.ZodMiniLiteral<"nsTypes">, z.ZodMiniLiteral<"duplicates">, z.ZodMiniLiteral<"enumMembers">, z.ZodMiniLiteral<"namespaceMembers">, z.ZodMiniLiteral<"catalog">, z.ZodMiniLiteral<"cycles">]> & z.core.$partial, z.ZodMiniEnum<{
         error: "error";
         off: "off";
diff --git a/dist/schema/configuration.js b/dist/schema/configuration.js
index 33117682cd2a94e159292496c729f408952739b2..e156b19947e2ce1aac95580b592fa3023523c995 100644
--- a/dist/schema/configuration.js
+++ b/dist/schema/configuration.js
@@ -36,6 +36,7 @@ const cyclesSchema = z.strictObject({
 });
 const rootConfigurationSchema = z.object({
     $schema: z.optional(z.string()),
+    conditions: z.optional(z.array(z.string())),
     rules: z.optional(rulesSchema),
     entry: z.optional(globSchema),
     project: z.optional(globSchema),
diff --git a/dist/types/config.d.ts b/dist/types/config.d.ts
index ba1f3edb632bb860487fa576d39a7f6a61fcddb7..85bda3e34afa25f8347e98276b0516ec70a7382b 100644
--- a/dist/types/config.d.ts
+++ b/dist/types/config.d.ts
@@ -42,6 +42,7 @@ export type GetImportsAndExportsOptions = {
     tags: Tags;
 };
 export interface Configuration {
+    conditions?: string[];
     ignore: NormalizedGlob;
     ignoreBinaries: IgnorePatterns;
     ignoreDependencies: IgnorePatterns;
diff --git a/dist/util/resolve.d.ts b/dist/util/resolve.d.ts
index 6b9bad57d42d68ad68eb19695dfe666ac045681f..2203605018e40fd5023e2e616299fdebe4205219 100644
--- a/dist/util/resolve.d.ts
+++ b/dist/util/resolve.d.ts
@@ -1,3 +1,4 @@
+export declare const setResolverConditions: (conditions: string[] | undefined) => void;
 export declare const _resolveModuleSync: (specifier: string, basePath: string) => string | undefined;
 export declare const _createSyncModuleResolver: (extensions: string[], tsConfigFile?: string) => (specifier: string, basePath: string) => string | undefined;
 export declare function clearResolverCache(): void;
diff --git a/dist/util/resolve.js b/dist/util/resolve.js
index 68625206d92e705ad6a16bf6e2c9fc43881cfb94..f8a692313988cecf765dd064e4d9b05fc56e4e61 100644
--- a/dist/util/resolve.js
+++ b/dist/util/resolve.js
@@ -8,12 +8,22 @@ const extensionAlias = {
     '.mjs': ['.mjs', '.mts', '.d.mts'],
     '.cjs': ['.cjs', '.cts', '.d.cts'],
 };
+const defaultConditionNames = ['require', 'import', 'node', 'default'];
+const browserConditionNames = ['require', 'import', 'browser', 'default'];
+let configuredConditionNames;
+export const setResolverConditions = (conditions) => {
+    configuredConditionNames = conditions;
+    resolveModuleSync = undefined;
+    resolveSync = undefined;
+    resolverInstances.length = 0;
+};
+const getConditionNames = (conditionNames) => configuredConditionNames ?? conditionNames;
 const resolverInstances = [];
 const createSyncModuleResolver = (extensions, tsConfigFile) => {
     const baseOptions = {
         extensions,
         extensionAlias,
-        conditionNames: ['require', 'import', 'node', 'default'],
+        conditionNames: getConditionNames(defaultConditionNames),
         nodePath: false,
     };
     const resolver = new ResolverFactory({
@@ -22,7 +32,7 @@ const createSyncModuleResolver = (extensions, tsConfigFile) => {
     });
     const fallbackResolver = new ResolverFactory({
         ...baseOptions,
-        conditionNames: ['require', 'import', 'browser', 'default'],
+        conditionNames: getConditionNames(browserConditionNames),
     });
     resolverInstances.push(resolver, fallbackResolver);
     return function resolveSync(specifier, basePath) {
@@ -36,13 +46,17 @@ const createSyncModuleResolver = (extensions, tsConfigFile) => {
         }
     };
 };
-const resolveModuleSync = createSyncModuleResolver([...DEFAULT_EXTENSIONS, ...DTS_EXTENSIONS, '.json', '.jsonc']);
-export const _resolveModuleSync = timerify(resolveModuleSync, 'resolveModuleSync');
+const defaultModuleExtensions = [...DEFAULT_EXTENSIONS, ...DTS_EXTENSIONS, '.json', '.jsonc'];
+let resolveModuleSync;
+export const _resolveModuleSync = timerify(function resolveModuleSync_(specifier, basePath) {
+    resolveModuleSync ??= createSyncModuleResolver(defaultModuleExtensions);
+    return resolveModuleSync(specifier, basePath);
+}, 'resolveModuleSync');
 export const _createSyncModuleResolver = (extensions, tsConfigFile) => timerify(createSyncModuleResolver(extensions, tsConfigFile), 'resolveModuleSync');
 const createSyncResolver = (extensions) => {
     const resolver = new ResolverFactory({
         extensions,
-        conditionNames: ['require', 'import', 'node', 'default'],
+        conditionNames: getConditionNames(defaultConditionNames),
         nodePath: false,
     });
     resolverInstances.push(resolver);
@@ -56,5 +70,9 @@ export function clearResolverCache() {
     for (const resolver of resolverInstances)
         resolver.clearCache();
 }
-const resolveSync = createSyncResolver([...DEFAULT_EXTENSIONS, '.json', '.jsonc']);
-export const _resolveSync = timerify(resolveSync);
+const defaultExtensions = [...DEFAULT_EXTENSIONS, '.json', '.jsonc'];
+let resolveSync;
+export const _resolveSync = timerify(function resolveSync_(specifier, baseDir) {
+    resolveSync ??= createSyncResolver(defaultExtensions);
+    return resolveSync(specifier, baseDir);
+});
diff --git a/schema.json b/schema.json
index f3cfd9a35d3c7792a9ff516ed7e72b71987aec26..6467e0b99e3878880ad63bc38689f306ef0e1a26 100644
--- a/schema.json
+++ b/schema.json
@@ -21,6 +21,12 @@
     }
   ],
   "properties": {
+    "conditions": {
+      "title": "Export/import map conditions to use during module resolution",
+      "description": "Replaces Knip's built-in condition names for all module resolvers. The node defaults are require, import, and node; the browser fallback uses require, import, and browser. Package map default targets remain fallbacks. Matching conditions follow map key order, not array order.",
+      "examples": [["source", "require", "import", "node", "default"]],
+      "$ref": "#/definitions/list"
+    },
     "ignoreBinaries": {
       "$ref": "#/definitions/ignoreBinaries"
     },
